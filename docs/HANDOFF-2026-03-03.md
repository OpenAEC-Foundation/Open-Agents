# Handoff — Sessie 2026-03-03

> **Geschreven door**: Claude Code (Opus 4.6) als orchestrator
> **Agents gebruikt**: 9 (3 audit, 3 fix/refactor, 1 research, 1 UI design, 1 GitHub admin)

---

## Wat er vandaag is gebeurd

### Commits (4, allemaal gepusht)

1. **`refactor: move 10 docs to docs/, archive stale files`** — Root opschoning: AGENTS, MASTERPLAN, DECISIONS, ROADMAP, etc. verplaatst naar `docs/`. HTML en test-workspace gearchiveerd. Root van 26 naar 17 bestanden.

2. **`feat(oa-cli): major refactor — split orchestrator, remove proposals, add security`** — Proposals volledig verwijderd (L-018). orchestrator.py (560 regels) opgesplitst in tmux.py, spawner.py, lifecycle.py. Nieuwe utils.py en config.py. File locking op agents.json. Input validatie op agent-namen.

3. **`feat(web): tabbed UI with dashboard, agent builder, and settings`** — 39 nieuwe bestanden. Volledige tabbed interface: Dashboard, Agent Builder (React Flow canvas), Templates, Context Builder, Settings. Zustand stores, Tailwind styling.

4. **`ci: add Python linting workflow for oa-cli`** — Ruff linter via GitHub Actions.

### Overige acties
- GitHub repo description + topics bijgewerkt
- 4 nieuwe labels toegevoegd (python, cli, testing, ci/cd)
- Project board "Open-Agents Development" aangemaakt

---

## Concrete Actiepunten

### P0 — Nu doen (blokkerend)

| # | Actie | Waarom | Bron |
|---|-------|--------|------|
| 1 | **Inter-agent messaging bouwen** | Agents kunnen niet communiceren. Cruciaal voor complexe taken. User wil dit als eerste. | L-023, user request |
| 2 | **Web UI testen en fixen** | 39 nieuwe componenten, nog niet live getest. Build slaagt maar runtime errors mogelijk. | design-ui agent |
| 3 | **CI/CD fixen** | User meldt warnings op GitHub Actions. Check of python-lint.yml en ci.yml correct draaien. | user feedback |

### P1 — Korte termijn (deze week)

| # | Actie | Waarom | Bron |
|---|-------|--------|------|
| 4 | **Live preview panel** | Gebruikers willen direct zien wat agents produceren. Bolt.new en Lovable zijn succesvol OMDAT ze dit hebben. | competitive research |
| 5 | **@ Mention context systeem** | `@file:auth.py`, `@agent:reviewer` — chirurgische context controle. Cursor's gouden standaard. | competitive research |
| 6 | **Lokale LLM routing** | Models <1B params voor simpele taken. Ollama cross-platform fixen. Bespaart subscription tokens. | user request, audit-code |
| 7 | **Customizable UI layouts** | Users moeten kiezen: kolom-view, flowchart-view, kanban-view. Inspiratie: draw.io. | user request |

### P2 — Middellange termijn (komende 2 weken)

| # | Actie | Waarom | Bron |
|---|-------|--------|------|
| 8 | **Peer-to-peer agent messaging op canvas** | Visualiseer agent communicatie als labels op edges. Iets wat CC Agent Teams niet kan. | competitive research |
| 9 | **Agent Task Board (Kanban)** | Visueel task board naast canvas. Agents claimen taken autonoom, dependencies zichtbaar. | competitive research |
| 10 | **Checkpoint/Restore** | Automatische snapshots bij elke agent actie. Terugkeren naar elk punt. Bouwt vertrouwen. | competitive research (Cline) |
| 11 | **Cost tracking dashboard** | Token usage per agent, per pipeline, per model. Budgetlimieten per node. | competitive research |
| 12 | **`pip install open-agents`** op PyPI | Laagdrempelig installeren voor iedereen. | user request |
| 13 | **packages/cli archiveren** | Legacy, volledig vervangen door oa-cli. | audit-convergence |

### P3 — Lange termijn

| # | Actie | Waarom | Bron |
|---|-------|--------|------|
| 14 | **Git integration + PR triggers** | `@open-agents` label op issue → automatische pipeline. | competitive research |
| 15 | **Visual editing op agent output** | WYSIWYG editor op live preview. Klik op element → pas aan. | competitive research (Lovable) |
| 16 | **Agent library uitbreiden** | 90 → 300+ agents. Meer categorieën, meer specialisaties. | roadmap |
| 17 | **oa-cli × packages/ convergentie** | Unified agent state, shared types. Sprint 15 in MASTERPLAN. | audit-convergence |

---

## Open Vragen

| # | Vraag | Context |
|---|-------|---------|
| Q1 | **Hoe implementeren we inter-agent messaging?** Shared file? Unix socket? In-memory bus? | Moet werken met tmux-geïsoleerde agents. CC Agent Teams gebruikt file-based approach. |
| Q2 | **Willen we draw.io-achtige functionaliteit?** Vrije tekenfunctie, shapes, connectors? Of alleen agent nodes? | User noemt draw.io als inspiratie. React Flow kan veel maar is geen vrij tekenprogramma. |
| Q3 | **Hoe routeren we taken naar lokale LLM's?** Automatisch op basis van complexiteit? Of user kiest per task? | Models <1B params zijn beperkt. Slimme routing nodig. |
| Q4 | **PyPI publicatie — onder welke naam?** `open-agents`? `oa-cli`? `open-agents-cli`? | `open-agents` is misschien al bezet op PyPI. |
| Q5 | **Moet de web UI standalone draaien (zonder oa-cli)?** Of altijd als onderdeel van `oa web`? | Standalone = meer bereik maar meer werk (eigen backend nodig). |

---

## Codebase Staat Na Vandaag

### oa-cli/src/open_agents/ (15 modules, ~2900 regels)
```
__init__.py        (3)    — versie
auto_manager.py    (149)  — background polling
bridge.py          (165)  — Flask REST API (proposals verwijderd)
chat.py            (245)  — interactieve REPL
cli.py             (330)  — Typer CLI commands (review/apply verwijderd)
config.py          (NEW)  — ~/.oa/config.json lezer
dashboard.py       (420)  — Textual TUI
lifecycle.py       (NEW)  — agent check, cleanup, kill
monitor.py         (120)  — Rich status tabel
orchestrator.py    (130)  — alleen orchestrator-specifiek
pipeline.py        (255)  — planner → workers → combiner
preflight.py       (131)  — dependency checks
spawner.py         (NEW)  — agent spawn logica
state.py           (230)  — JSON state + file locking
tmux.py            (NEW)  — tmux session/window ops
utils.py           (NEW)  — generate_name, format_duration, validate_name
workspace.py       (100)  — workspace builder (proposals verwijderd)
```

### oa-cli/web/src/ (39 componenten)
```
components/
  builder/     — FlowCanvas, NodePalette, 4 node types
  context/     — ContextTab, ContextWorkspace
  dashboard/   — AgentList, AgentDetail, SpawnForm, SystemHealth, ActivityFeed
  layout/      — Header, TabBar
  settings/    — SettingsTab, ModelConfig, ApiKeyManager
  shared/      — Card, EmptyState, ModelBadge, StatusBadge, Terminal
  templates/   — TemplatesTab, TemplateCard
stores/        — agentStore, contextStore, settingsStore, templateStore, uiStore
types/         — TypeScript type definitions
api/           — REST client
```

---

## Lessen van Vandaag

| # | Les |
|---|-----|
| L-030 | **Parallel agents op dezelfde bestanden kan werken** — mits de scopes voldoende gescheiden zijn (proposals verwijderen ≠ utils extraheren ≠ modules splitsen). Maar het is risicovol. Beter: sequentieel of git worktrees. |
| L-031 | **`oa run --direct` is de standaard** — proposal mode is afgeschaft. Agents schrijven direct. Geen `oa review` / `oa apply` meer. |
| L-032 | **9 agents is beheersbaar** — 3 audit (read-only) + 3 fix (code-wijzigend) + 3 research (read-only). Mix van read-only en write agents reduceert conflicten. |
| L-033 | **Web UI dependencies niet vergeten** — `npm install` nodig na package.json wijzigingen door agents. `npm run build` na elke UI wijziging. |

---

*Sessie afgesloten: 2026-03-03*
*Impertio Studio B.V. — AI ecosystems, deployed right.*
