# HANDOFF — 2026-03-11c (Issue Triage Sessie)

> **Sessie type**: Issue triage + gerichte bugfixes
> **Volgende sessie leest dit als eerste**

---

## Wat is er gedaan

### GitHub Issues: 16 → 6 open

**Gesloten als stale (al geïmplementeerd):**
- #65 check-delegation.sh false positive — al opgelost op regel 38
- #67 `oa run --template` — al in cli.py
- #71 `oa mcp` + PyPI — al aanwezig
- #73 SpawnForm auth header — al `authHeaders()` correct
- #74 Bridge PATH fix — al in `run_bridge()`

**Gefixed en gesloten:**
- #64 Root-server spawn — root-detectie vóór SSH-spawn toegevoegd in `spawner.py`
- #66 Template schema — `_archive/` uitgesloten + schema-validatie in `template_loader.py`
- #75 Machines UI — SpawnForm verbergt machine-sectie bij geen remote machines
- #77 Remote duration 1s — gesloten als symptoom van #64

**Gesloten als volledig geïmplementeerd:**
- #69 Sprint-22 telemetry — alle 3 items aanwezig (start_run/finish_run, hooks, context_tracker)

**Open met commentaar + implementatie-roadmap:**
- #68 Docker isolation — `docker_runtime.py` bestaat, spawner-integratie ontbreekt
- #70 Terminal backend — Python kant klaar, packages/ TypeScript-kant niet
- #72 VS Code Bridge — vscode_bridge.py klaar, shared types + E2E pending
- #76 Ollama — text-only geïmplementeerd, agentic mode (tools) nog niet
- #78 /loop — `oa loop` CLI command nog niet gebouwd, sketch gegeven

### Code wijzigingen (gepusht)
- `oa-cli/src/open_agents/spawner.py` — root-detectie (#64)
- `oa-cli/src/open_agents/template_loader.py` — uitsluiting + validatie (#66)
- `oa-cli/web/src/components/dashboard/SpawnForm.tsx` — conditionele machine-sectie (#75)
- Web UI herbuild: `oa-cli/web/dist/` bijgewerkt

### Core bestanden bijgewerkt
- `LESSONS.md` — L-077 t/m L-082 toegevoegd
- `DECISIONS.md` — D-073, D-074, D-075 toegevoegd
- `CHANGELOG.md` — Unreleased sectie bijgewerkt
- `ROADMAP.md` — status bijgewerkt (16 → 6 issues)

---

## Wat open staat

### Prioriteit 1 — Implementeren (meeste impact)
| Issue | Wat te doen | Geschatte omvang |
|-------|-------------|------------------|
| #68 Docker | `oa run --docker` flag + spawner-integratie + Dockerfile | ~1 dag |
| #78 /loop | Nieuwe `oa loop` CLI command + interval parsing + --remote | ~2-3u |
| #76 Ollama agentic | Native ReAct-loop met Ollama `/api/chat` + tools | ~2 dagen |

### Prioriteit 2 — Completeren (gedeeltelijk klaar)
| Issue | Wat te doen |
|-------|-------------|
| #72 VS Code Bridge | Shared types merge packages/shared → vscode-extension |
| #70 Terminal | packages/ Fastify node-pty backend + Tauri CI |

### Prioriteit 3 — Grote features (volgende sprints)
| Issue | Sprint |
|-------|--------|
| #63 Chat UI | Sprint 23+ |

---

## Repo state

```
Branch: main
Last commit: fix(ui): hide machine selector when no remote machines configured (#75)
Open issues: 6 (alle enhancement, geen bugs meer)
Test status: geen failing tests bekend
```

---

## Aanbevolen volgende stap

**`oa loop` is de laaghangende vrucht** — contained feature (~50 regels), hoge gebruikswaarde (recurring health checks, benchmarks, polling), duidelijke spec uit issue #78.

Start met: `oa run "Implementeer oa loop CLI command in cli.py" --name loop-impl --model claude/sonnet --direct`
