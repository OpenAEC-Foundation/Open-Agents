# HANDOFF 2026-03-12c

## Sessie samenvatting

Sprint 29 (Reproducible Foundation) volledig afgerond. Task-type templates, output contracts, CI pipeline, en remote path-mapping zijn geïmplementeerd en getest. Hetzner infrastructure setup gedocumenteerd. Headless Claude-auth script verbeterd.

## Wat is gebouwd

### 1. Task-type templates (6×) — `templates/task_types/`
- Types: `researcher`, `builder`, `reviewer`, `transformer`, `orchestrator`, `validator`
- Elke template injecteert rolbeschrijving, input-contract en output-regels in agent workspace
- `oa run --type <type>` activeert de bijbehorende template
- Maakt agent-output reproduceerbaar zonder AI-intelligentie te beperken (L-098)

### 2. Output contracts — `output_contracts.py`
- `validate_output_contract()` — verifieert aanwezigheid van secties en formaat
- 24/24 pytest tests groen
- Contract-validatie wordt automatisch uitgevoerd in `lifecycle.py` na agent completion

### 3. CI Pipeline — `.github/workflows/python-test.yml`
- GitHub Actions matrix: Python 3.10 en 3.11
- Alle tests draaien automatisch bij push/PR

### 4. Remote path-mapping — `spawner.py`
- `_map_paths_for_remote()` — vertaalt WSL/Windows paden naar Ubuntu-equivalenten
- `_ensure_remote_repo()` — synchroniseert repo automatisch naar Hetzner via git
- `config.py`: nieuwe sleutels `remote_repo_path`, `remote_wsl_path_prefix`, `github_pat`
- Fundamentele fix: WSL/Windows en Ubuntu paden zijn niet uitwisselbaar (D-081)

### 5. Hetzner Infrastructure
- `scripts/setup-hetzner-repo.sh` — idempotent setup script voor Hetzner
- `docs/HETZNER-SETUP.md` — volledige setup handleiding
- `scripts/claude-auth-headless.py` — `--check-only` flag + retry-logica

### 6. Pre-flight auth check — `spawner.py`
- Auth-check via echte API call vóór agent spawn (niet `auth status`, maar werkelijke API)
- Voorkomt stille 401-fouten en geeft directe feedback over verlopen token

## Commits deze sessie

- `feat(sprint29): task-type templates + output contracts + CI + remote path fix` (88ea6e7)
- `feat(sprint29): --type flag, contract validation on done, Hetzner setup docs` (9243bc7)
- `docs(sprint29): ROADMAP v0.3.3 + D-080/D-081 + e2e test report` (375ba6c)

## State

- Branch: main, up-to-date met remote
- 24/24 output contract tests groen
- Python 3.10/3.11 CI groen
- Hetzner SSH alive; Claude CLI geauthenticeerd (freek@3bm.co.nl, Max subscription)

## Openstaand

- **Hetzner OAuth token verlopen** — handmatige browser-klik vereist (`claude auth login` in terminal, NIET via Claude Code chat). Zie L-100.
- **Hetzner git clone niet getest** — private repo + GitHub PAT vereist configuratie in `~/.oa/config.json` (`github_pat`, `remote_repo_path`)
- **Sprint 30** — nog niet gestart

## Volgende sessie

- Hetzner auth handmatig vernieuwen via `claude auth login`
- End-to-end test: `oa run --type researcher "..." --model claude/sonnet --direct` op Hetzner
- Git-clone flow testen: `_ensure_remote_repo()` met PAT-configuratie
- Sprint 30 plannen (scope nog open)
