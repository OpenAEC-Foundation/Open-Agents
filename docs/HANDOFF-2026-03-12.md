# HANDOFF — 2026-03-12

## Sessie samenvatting
Filosofische verdieping (recursive agent tree als kernfilosofie) gecombineerd met concrete implementatie: validatieloops in orchestrator templates, remote-first execution naar Hetzner, en MASTERPLAN/core-bestanden gesynchroniseerd.

---

## Wat gedaan is

### Visie & Documentatie
- **D-060**: Recursive Agent Tree als kernfilosofie gedocumenteerd — PRINCIPLES-FOUNDATION.md sectie 3.4.1, DECISIONS.md, kernprincipes 8+9
- **MASTERPLAN v0.8**: 11 sprint-statussen gesynchroniseerd, Sprint 27 toegevoegd, Sprint 28 (Live Agent Tree Visualisatie) als placeholder
- **LESSONS L-093-095**: Remote execution kennis vastgelegd

### Orchestratie-kwaliteit
- Validatieloop toegevoegd aan alle orchestrator templates (session-start, delegate, patterns, pipeline, spawn)
- `contract.py` — output contract verificatie per task-type (PASS/FAIL)
- `oa-cli/templates/orchestrator-loop.md` — uitvoerbare referentie voor iteratieve kwaliteitslus

### Remote-First (D-061)
- `machines.json` aangepast: `hetzner is_default=true`
- `config.py`: `get_default_machine()` helper
- `agents.py` + `pipeline.py`: `--local` flag toegevoegd, remote-first routing
- Gedragsmatrix: `oa run` → Hetzner | `oa run --local` → lokaal | `oa run --remote X` → X

### Sprint 28 plan
- `docs/design/sprint28-live-tree-plan.md` — uitvoerbaar 4-fase implementatieplan voor live agent tree visualisatie (React Flow)

---

## Beslissingen genomen

| # | Beslissing |
|---|-----------|
| D-060 | Recursive Agent Tree als kernfilosofie — bottom-up propagation, peer-verificatie, live visualisatie |
| D-061 | Remote-first execution — Hetzner default voor alle `oa run`, ook claude/haiku/sonnet/opus |

---

## Open punten (prioriteit)

1. **🔴 BLOCKER: OAuth token Hetzner verlopen** — `ssh hetzner-agent "claude auth login"` → browser → Approve → code plakken. Remote-first werkt pas daarna.
2. **Sprint 28 bouwen** — Live Agent Tree Visualisatie (plan klaar: `docs/design/sprint28-live-tree-plan.md`)
3. **Template cleanup** — 1626 → ~200 templates (rapport klaar: `docs/template-consolidation-report.md`)
4. **Playwright OAuth auto-script** — automatisch token vernieuwen bij verloop (SSH tunnel + browser automation)
5. Remote tmux sessions op Hetzner (agents nu via SSH background process, geen tmux window)
6. `oa status` local/remote split

---

## Volgende sessie — eerste stap

```bash
# 1. OAuth vernieuwen (vereist browser)
ssh hetzner-agent "claude auth login"
# → open URL in browser → Approve → plak code terug

# 2. Testen
oa run "echo hello" --name test-remote --model claude/sonnet --direct
oa collect test-remote

# 3. Dan: Sprint 28 starten
# Plan staat in: docs/design/sprint28-live-tree-plan.md
```

---

## Technische staat

| Component | Status |
|-----------|--------|
| oa-cli remote-first code | ✅ Geïmplementeerd |
| machines.json hetzner default | ✅ Actief |
| Claude CLI op Hetzner | ❌ OAuth verlopen — moet vernieuwd |
| Orchestrator validatieloop templates | ✅ Geïmplementeerd |
| Sprint 28 implementatieplan | ✅ Klaar |
| Contract verificatie (contract.py) | ✅ Geïmplementeerd |
